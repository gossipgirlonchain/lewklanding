{
  "name": "lewklanding",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd apps/web && npm install && npm run build"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  }
}

